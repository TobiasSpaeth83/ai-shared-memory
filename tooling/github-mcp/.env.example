# GitHub App Configuration
# Copy this file to .env and fill in your actual values

# GitHub App ID (found in your GitHub App settings)
GITHUB_APP_ID=your_app_id_here

# GitHub Installation ID (found after installing the app on a repository)
GITHUB_INSTALLATION_ID=your_installation_id_here

# GitHub App Private Key (download from GitHub App settings)
# Format: The entire PEM key including BEGIN and END lines
# You can also use a file path: GITHUB_PRIVATE_KEY_PATH=/path/to/private-key.pem
GITHUB_PRIVATE_KEY="-----BEGIN RSA PRIVATE KEY-----
your_private_key_content_here
-----END RSA PRIVATE KEY-----"

# Repository Allowlist (comma-separated)
# Format: owner/repo,owner2/repo2
# Leave empty to allow all repositories the app has access to
REPO_ALLOWLIST=your-org/ai-shared-memory

# Optional: Log level (debug, info, warn, error)
LOG_LEVEL=info